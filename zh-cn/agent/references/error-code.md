---
description: "Qingkuai 错误码参考：1xxx 编译错误、9xxx 编译警告、2xxx 运行时错误、8xxx 运行时警告、3xxx/7xxx 语言服务消息。"
keywords: ["error code", "compilation error", "warning", "runtime error", "1070", "报错", "错误码"]
---

# 错误码参考

错误码按号段分组：**1xxx** 编译错误（阻断编译）、**9xxx** 编译警告（不阻断）、**2xxx** 运行时错误、**8xxx** 运行时警告、**3xxx** 语言服务错误、**7xxx** 语言服务警告。

## 编译错误（1xxx）

| 错误码 | 描述 |
|---|---|
| 1001 | 空的插值块 |
| 1002 | 非预期的字符 |
| 1003 | 未闭合的插值块 |
| 1004 | 以结束标签开头，且之前没有匹配的开始标签 |
| 1005 | 插值属性缺少属性名（只写了 `!`、`@`、`#` 或 `&`） |
| 1006 | 静态属性值未用引号包裹 |
| 1007 | 插值属性值未用花括号包裹 |
| 1008 | 静态属性值未闭合 |
| 1009 | 标签未闭合（开始/结束/注释标签） |
| 1010 | 嵌入语言标签未用在模板顶层 |
| 1011 | 嵌入脚本块过多（每个组件文件只允许一个） |
| 1012 | 标签缺少匹配的结束标签 |
| 1013 | 标签不能自闭合（如 `<div />`） |
| 1014 | 使用了组件文件中不允许的标签 |
| 1015 | 非法的模板结构（如 `<p>` 内嵌套 `<div>`） |
| 1016 | 属性格式无效 |
| 1017 | 使用了框架保留的标识符格式（`__qk__` 前缀） |
| 1018 | 不支持的顶层 `await` 表达式 |
| 1019 | 不支持的导出形式（`export =`、默认导出、再导出、命名空间/类型导出） |
| 1020 | 内建标识符在顶层作用域被重复声明 |
| 1021 | 内建方法用在无效位置或调用形式 |
| 1022 | 标识符与别名或衍生值标记冲突，无法重复声明 |
| 1023 | `defaults` 内建方法在嵌入脚本块中只能调用一次 |
| 1024 | `alias` 参数无效；必须接收恰好一个可写目标 |
| 1025 | 别名解构声明包含不允许的模式 |
| 1026 | 指令冲突；不能共存于同一标签 |
| 1027 | 指令缺少必需的值 |
| 1028 | 属性重复或解析后冲突（静态/动态/事件/引用属性之间） |
| 1029 | 表达式无效 |
| 1030 | 标签不接受当前属性类型 |
| 1031 | 缺少前置指令（`#elif`、`#else`、`#then`、`#catch`） |
| 1032 | 上下文模式无效 |
| 1033 | 无法识别的指令 |
| 1034 | 上下文模式为空，未声明绑定标识符 |
| 1035 | `#html` 指令标签必须包含恰好一个文本子节点 |
| 1036 | `#slot` 用在无效位置（只允许组件的一级子元素） |
| 1037 | 指令解构绑定模式过多（`#for`、`#then`） |
| 1038 | `#slot` 缺少合法的字符串字面量插槽名 |
| 1039 | `<slot>` 的 `name` 属性必须是静态值 |
| 1040 | `#target` 用在无效位置（组件的一级子元素） |
| 1041 | 期望一个表达式 |
| 1042 | 期望一个字符串字面量 |
| 1043 | `#key` 只能与 `#for` 配合使用 |
| 1044 | 期望事件标志名 |
| 1045 | 无法识别的事件标志 |
| 1046 | 事件标志冲突 |
| 1047 | 该标签上的引用属性无效（不在允许列表内） |
| 1048 | 引用属性值无效；必须是标识符或成员表达式 |
| 1049 | 省略形式的特殊属性名无效，无法转换为合法标识符 |
| 1050 | `<slot>` 上的 `name` 属性重复 |
| 1051 | 同一组件内对同一插槽名重复赋值 |
| 1052 | 嵌入脚本块不支持 TypeScript 命名空间声明 |
| 1053 | `alias` 不能为独立标识符创建别名 |
| 1054 | 内建方法不能用在 `using`/`await using` 声明中 |
| 1055 | 不允许嵌套 `<slot>` 标签 |
| 1056 | Promise 块中重复的 `#then` 或 `#catch` |
| 1057 | 组件名无效（无法转换为合法标识符或成员表达式） |
| 1058 | `#html` 不能用于组件或 `<slot>` 标签 |
| 1059 | 该内建方法不支持展开参数 |
| 1060 | 元素标签名无效 |
| 1061 | 内建方法不能用在模板中 |
| 1062 | 响应性模式冲突（同一标签同时声明 `reactive` 和 `shallow`） |
| 1063 | 组件标签上的泛型参数未闭合 |
| 1064 | 组件标签的泛型参数要求嵌入脚本语言为 TypeScript |
| 1065 | 成员表达式组件标签中不允许连字符 |
| 1066 | 自闭合的嵌入样式标签必须提供 `src` 属性 |
| 1067 | 带 `src` 属性的嵌入样式标签不能包含内联样式内容 |
| 1068 | 嵌入样式标签的 `src` 属性需要非空值 |
| 1069 | `#scope` 只能用于组件 |
| 1070 | `allowConstReactive` 编译选项禁用时，不允许用 `reactive`/`shallow` 标记 `const` 声明 |
| 1071 | `raw` 用作非响应式读取时必须传递参数 |
| 1072 | `raw` 用作非响应式读取时只能传递一个参数 |
| 1073 | `raw` 用作非响应式读取时必须用作函数调用 |
| 1074 | 编译选项 `requireReactivityMark` 为 `true` 时顶层变量声明必须使用响应性内建方法显式标记 |

## 编译警告（9xxx）

| 错误码 | 描述 |
|---|---|
| 9001 | 值从不变化；响应性标记冗余，将按 raw 处理 |
| 9002 | 顶层标识符可能在特定作用域中被遮蔽 |
| 9003 | `#scope` 无效：当前组件没有作用域样式 |
| 9004 | 衍生值是只读的，可变声明冗余；建议使用 `const` |
| 9005 | 对字面量 `const` 应用 `raw` 是冗余的 |
| 9006 | 该指令不需要值；提供的值将被忽略 |
| 9007 | 布尔属性被赋予了冗余值；将被忽略 |
| 9008 | `#html` 无值且内容为静态时没有实际作用 |
| 9009 | 组件事件监听上的事件标志无效；将被忽略 |
| 9010 | 非键盘事件上的键盘事件标志；将被忽略 |
| 9011 | 重复的事件标志；将被忽略 |
| 9012 | 缺少必需部分（动态属性、引用属性或事件监听）的 `<qk:spread>` 是多余的 |
| 9013 | `#scope` 多余：该组件已有真实祖先元素 |
| 9014 | 嵌套的 `raw` 调用冗余：参数已是非响应式读取，内层 `raw` 会被忽略 |
| 9015 | 未加 `#key` 的 `#for` 列表项含内部状态，列表更新时可能串位 |

## 运行时错误（2xxx）

| 错误码 | 描述 |
|---|---|
| 2001 | 特定用法（如 `#await`）接收的值不是 `Promise` |
| 2002 | `#for` 的值不可迭代 |
| 2003 | `#key` 的值存在重复 |
| 2004 | 超过最大递归更新深度（常见于异步副作用或监视器中的递归更新） |
| 2005 | 目标元素无效（不是 `Element`，或选择器找不到元素） |
| 2006 | `&group` 或多选 `<select>` 的 `&value` 必须是数组或 `Set` |
| 2007 | 无法渲染：给定值既不是组件函数，也不是兑现为组件函数的 `Promise` |

## 运行时警告（8xxx）

| 错误码 | 描述 |
|---|---|
| 8001 | 副作用/监视器执行时未收集到任何响应式依赖；该副作用将被销毁 |
| 8002 | 对只读或无效目标进行了赋值；赋值将被忽略 |
| 8003 | 在所属组件销毁后尝试创建内容（常见于组件移除后异步回调创建子组件或注册监视器/副作用）；创建已被忽略 |
| 8004 | 生命周期方法在对应阶段已过后注册（如组件挂载后在异步回调中注册 `onAfterMount`）；回调永不触发，注册已被忽略 |

## 语言服务（3xxx 错误 / 7xxx 警告）

| 错误码 | 描述 |
|---|---|
| 3001 | `Meta` 声明了未知成员（只允许 `props`、`refs`、`contexts`） |
| 3002 | `Meta` 或其成员不是对象类型（TypeScript） |
| 3003 | 找不到依赖 "qingkuai"；请确认已安装且可解析 |
| 3004 | 带泛型参数的导入类型被直接用作组件契约 `Meta` |
| 3005 | 类型不能从组件中导出；请移到外部 `.ts` 文件再导入 |
| 7001 | 经 JSDoc 声明的 `Meta` 不是对象类型（JavaScript） |
| 7002 | `@keyframes` 规则未被作用域化；建议在外部样式表中定义并从应用入口导入 |

## 参见

- [响应性推导规则](./reactivity-infer-rules.md)
- [配置文件](../misc/config-files.md)
